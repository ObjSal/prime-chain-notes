# NOTES — verified results & decisions

## What was proven, in order (2026-07-05)

1. **notes-core host tests** (`cargo test -p notes-core`, 14 tests): BIP340
   official vectors; BIP341 tweak vector; bech32m addresses equal
   rust-bitcoin's for mainnet/signet/regtest; envelope round-trips at chunk
   boundaries; AEAD seal/open + AAD binding (note_id) + tamper rejection;
   fee estimator byte-exact vs signed txs; full compose→scan round-trips
   (public/private/multi-chunk); spoof rejection (`spends_from_self=false`
   ignored); foreign-seed ciphertext yields `text=None`; idempotent import;
   and the heavyweight cross-check — rust-bitcoin parses our raw tx, agrees
   on txid+vsize, recomputes the BIP341 sighash with its own code, and
   libsecp256k1 accepts our schnorr signatures.

2. **Host e2e vs real bitcoind -regtest** (`scripts/regtest-e2e.sh`, Core
   v30.2): funding; private note (2×80-byte chunks); public note **chained
   onto unconfirmed change**; 4-chunk long note; **323-byte single
   OP_RETURN** (v30 datacarrier default accepted it — the 80-byte era is
   over on v30 nodes); `testmempoolaccept` clean; our txids byte-identical
   to the node's; full-history rescan recovered all 4 notes (texts, heights,
   visibility) from bare chain data; foreign seed reads public but cannot
   decrypt private notes; plaintext of a public note visible in the raw tx.

3. **Simulator UI e2e** (`../ui-automation/tests/chain-notes.sh`): the
   host mirrors the sim identity (hosted GetAppSeed = HMAC-SHA256(app-id,
   master_seed) — arg order verified in `KeyOS/os/security/src/hosted.rs`
   + `api/crypto`: key=app_id, data=seed; the sim's on-screen address
   matched the host derivation). Bundle seeded into the User volume;
   Sync→Import through real taps; compose with the **live keystroke cost
   line** ("35 bytes · 2 chunk(s) · ~233 vB · ~466 sats (~$0.47) @
   2 sat/vB"); confirm screen fee exactly matched the estimate (466);
   Sign & export wrote outbox hex to Internal + Airlock; the extracted hex
   **broadcast and mined on the real regtest node with the same txid the
   sim predicted**; then a full app-state wipe + fresh bundle import
   restored the private note from chain, decrypted, "confirmed at block
   103". Screenshots in `screenshots/`.

## Decisions & gotchas discovered

- **Scaffold gaps**: `foundation new` (this run) did not create the
  `ui/ui` → SDK symlink nor `resources/{fonts,images,icons}` — copied the
  wiring from prime-bip85. Also the theme-menu expect delay needed **5s**
  (3s failed once; workspace CLAUDE.md updated).
- **manifest.toml** is only (re)generated by `foundation build/sim` — plain
  `cargo check` fails on a fresh clone until one `foundation sim` has run.
- **ui-automation chrome detection vs dark mode**: `sim_detect_scale`'s
  "first dark row" scan misreads dark-mode title bars (returned 1px). For
  self-calibrated coordinates use `SIM_CHROME_PX=72`, which makes
  `sim_tap_px` an exact identity map from live-capture pixels — correct in
  any appearance mode.
- **Change output position** is deterministic: outputs are [OP_RETURNs...,
  change], so change vout == chunk count — the app's ledger uses that when
  crediting its own unconfirmed change.
- Notes store keeps **plaintext locally** (device storage is the trust
  boundary; the chain copy is the encrypted one). Private notes recovered
  after a wipe decrypt from chain via the re-derived enc key.
- bitcoin-cli asm for nulldata is `OP_RETURN <hex>` — the companion takes
  the last asm token as the payload; `spends_from_self` is computed by
  resolving each input's prevout address (needs `-txindex` on regtest;
  mempool.space has this by construction).
- `notes_cli` (example) is host-only tooling; the shipped app never reads
  `NOTES_APP_SEED`.

## Fee-rate field + Custom tier (2026-07-05, user request)

Compose gained a sat/vB text field under the tier pills and a 4th
**Custom** pill: tapping Economy/Normal/Fast writes that tier's bundle
rate into the field; typing in the field auto-selects Custom and the
entered rate drives the estimator and the built tx. Verified in-sim
(screenshots `cn2-fast.png` / `cn2-custom.png`: Fast → "3", "@ 3 sat/vB";
manual "30" → Custom highlighted, "@ 30 sat/vB") and in the automated
test (fee == 3 × vsize after tapping Fast). At the same time
`regtest-companion.sh` bundles now advertise `max_op_return_bytes:
100000` — Core v30's actual default relay policy (v30.2 confirmed
locally; the earlier 80 was an over-conservative placeholder), so
typical notes are single-OP_RETURN; 80-byte chunking remains the
fallback for strict endpoints via the same bundle field.

## Settings screen split (2026-07-05, user request)

The chunk picker initially landed on Sync next to Import — the user
correctly flagged the action/preference mix. Screen 6 "Settings" now holds
the Network toggle + chunk picker (+ explainer); Sync is pure workflow
(status, Import, Export, result) with Import back near the top; Home
gained a fourth "Settings" button. Chunk changes still reprice a compose
draft instantly (`chunk_changed` invokes `compose-changed`; verified with
the same 100-byte draft going 1 chunk/~550 sats ↔ 3 chunks/~644 sats).

## Chunk-size picker (2026-07-05, user request)

Originally on Sync (since moved — see above); gained an OP_RETURN
chunk-size row: **Auto** (follow the
bundle's `max_op_return_bytes`) / **80 compat** / **Custom** bytes field
that auto-selects Custom on edit. Custom is validated to
`[20, max_op_return_bytes]` — by explicit decision it can never exceed
the endpoint policy; invalid input shows inline in the section label (the
keyboard covers the result line) and is not persisted. `effective_chunk()`
also clamps a saved override if a later import lowers the policy. Lives on
Sync, not Compose: compose is at its keyboard-safe layout limit, and chunk
size is endpoint policy, not a per-note choice — the compose cost line
still shows the resulting chunk count live. Verified in-sim (80-compat →
`cb: set-chunk-size 80 ok`; typing 800000 with max 100000 → inline error,
not persisted, earlier valid 80000 retained) and in the automated e2e
(80-compat via pill tap → 100-byte private note composes as exactly
3 chunks, fee == 3 × vsize at the Fast tier, mined on regtest).

## Not yet done / next

- Companion web pages (`notes-sync.html`, `broadcast.html`) — phase 2 of
  `../PLAN-chain-notes.md`; the regtest scripts stand in for them today.
- QR sync path (animated UR out, `open_qr_scanner` in) — file/Airlock is
  the only transport wired so far.
- Signet dry-run + Foundation security review before any mainnet use.
- Fee-bump (RBF) UI; the tx already signals RBF (sequence 0xfffffffd) and
  note_id survives re-signing by design.
